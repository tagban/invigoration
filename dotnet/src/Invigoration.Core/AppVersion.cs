namespace Invigoration.Core;

/// <summary>Single source of truth for the app's version string — shown in the window title bar (Windows/macOS) and the "!ver"/"!idle ver" bot command.</summary>
public static class AppVersion
{
    public const string Current = "2.0.4b";
}

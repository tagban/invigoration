using Avalonia.Controls;

namespace Invigoration.App.Views;

/// <summary>Empty placeholder — see MusicPlayerPanel's remarks for why the real player lives elsewhere.</summary>
public partial class MusicTabView : UserControl
{
    public MusicTabView()
    {
        InitializeComponent();
    }
}
